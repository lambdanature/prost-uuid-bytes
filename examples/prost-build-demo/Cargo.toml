[package]
name = "prost-build-demo"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
prost = "0.14"
prost-uuid-bytes = { path = "../.." }
uuid = { version = "1", features = ["v7"] }

[build-dependencies]
prost-build = "0.14"
prost-uuid-bytes = { path = "../..", default-features = false }
